# Exclude only the giant source zip (redundant with the extracted
# data/digitalfire-archive/ — re-download from archive.org if needed),
# plus scratch QA files and run logs. Everything else under data/ is committed
# so the whole archive can be cloned in one go.
/data/digitalfire-archive.zip
/data/qa/
/data_run.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editor / OS
.DS_Store
*.swp
